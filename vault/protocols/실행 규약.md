---
type: protocol
updated: 2026-09-15 19:19
tags: [hgnn/protocol]
---

<!-- AUTO:BEGIN -->
# 실행 규약

<small>공통 규칙. 모델별 명령은 각 `<모델> 실행` 노트에. 갱신 2026-09-15 19:19</small>

## 정식 실행이란

- 전임자 `<모델>/118.sh` 의 **고정 설정** · 기본 **200 epoch** · **20 split/seed**. 이것만 원장(Table 3·4·5)에 올라간다.
- smoke(1 seed · 15 epoch)는 실행 가능성 확인용이다. 원장에 쓰지 않는다.
- 노드 분류 분할은 `data_split_0.01.pickle` (train 1% / valid 1% / test 98%) — HyperGC 4장 프로토콜. `data_split_118.pickle`(10/10/80)은 Table 3 대비가 성립하지 않는다.
- 하이퍼엣지 예측 분할은 60 / 20 / 20, 음성 하이퍼엣지는 정답과 같은 수, 지표는 AUROC.

## 환경

| 환경 | requirement | 모델 |
|---|---|---|
| `hgnn-pyg` | `others_require.txt` (compatibility port) | AllSet, EDHNN, H-GD, Hypeboy, HyperGCN, MLP, MaskGAE, PhenomNN, TriCL, UniGCN2 |
| `hgnn-dgl-src` | `graphmae2_require.txt` (isolated source-build port) | GraphMAE2, HyperGRL |
| `hgnn-legacy` | `HGNN_require.txt` (compatibility port) | HGNN, HNHN, UniGCN, UniGIN, UniGCN2 |
| `hgnn-hypergcl` | `others_require.txt` baseline (compatibility port) | HyperGCL |

실행은 `conda run --no-capture-output -n <env> bash -lc "<명령>"`. GPU 는 `--gpu`/`--device` 인자 또는 `CUDA_VISIBLE_DEVICES`.

## 실행 스크립트 규약 (`.agents/run-scripts/*.sh`)

- `run_id="<이름>-gpu<N>-<YYYYMMDD>"`, 로그는 `.agents/env-status/full-runs/<run_id>/<dataset>.log`
- 데이터셋 하나가 끝날 때마다 `status.tsv` 에 `모델\t데이터셋\t상태\t시각` 을 append. 상태: `COMPLETE` · `FAILED_*` · `OOM` · `OOT` · `SKIP_EXISTING`
- 결과 파일이 이미 있으면 건너뛴다 (`SKIP_EXISTING`). 결과는 `results/result_<dataset>_<Dir>_<task>.txt`
- `timeout 24h` 로 감싼다. 종료 코드 124 = OOT.
- 다른 실행을 기다릴 땐 `pgrep -f '[스]크립트명'` 루프, env-checker 승인은 `*-check.ok` 파일 존재로 판단한다.

## 돌리지 말 것 (논문이 O.O.M / O.O.T 로 보고)

- HyperGRL × Citeseer (NC, OOM)
- HyperGRL × IMDB (NC, OOM)
- HyperGRL × Pubmed (NC, OOM)
- HyperGRL × AMiner (NC, OOM)
- PhenomNN × AMiner (NC, OOM)
- PhenomNN × AMiner (HP, OOM)
- PhenomNN × DBLP-P (HP, OOM)
- PhenomNN × 20News (HP, OOT)
- MaskGAE × 20News (HP, OOT)
- HyperGCL × DBLP-P (HP, OOM)
- HyperGRL × 20News (HP, OOT)
- MaskGAE × 20News (CD, OOT)
- HyperGCL × DBLP-P (CD, OOM)
- HyperGRL × 20News (CD, OOT)

## 데이터셋 이름

| 원시 이름 | 논문 표기 |
|---|---|
| citeseer_cite | Citeseer |
| cora_coauth | Cora-CA |
| imdb | IMDB |
| house | House |
| pubmed_cite | Pubmed |
| aminer | AMiner |
| dblp_copub | DBLP-A |
| dblp_coauth | DBLP-A |
| dblp_p | DBLP-P |
| modelnet_40 | MN-40 |
| news | 20News |
| cora_cite | Cora |

## 결과가 원장에 오르는 길

`results/*.txt` → `tools/refresh_all.sh` 가 1분마다 한 줄로 돌린다: `clerk_refresh.py` 가 `experiment_now.md` 에 값·Δ 기록 → `collector.py` 가 `state.json` → `vault_build.py` 가 [[논문 대조]] · `knowledge/models/` 갱신 → 바뀐 노트만 커밋·푸시.
실험 직후 노트에 안 보이면 어느 단계에서 멈췄는지 `dashboard/pipeline.log` 로 본다.
<!-- AUTO:END -->

## 함정과 판단

<!-- 클로드·사람이 실험하며 알게 된 것을 여기 쌓는다. 위 AUTO 구간은 덮어써진다. -->

