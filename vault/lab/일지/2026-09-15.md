---
type: lab
date: 2026-09-15
tags: [hgnn/lab]
---

# 2026-09-15

## 한 일

- **연구 노트북 구조 도입** · 사람용 대시보드 대신 클로드가 읽고 쓰는 `vault/` 로 재편. `knowledge/`(자동) · `protocols/`(정식 명령 + 함정) · `lab/`(일지·할 일·질문). 규칙은 레포 루트 `CLAUDE.md`.
- **GitHub 연결** · `khojin01/hgnn` (private). 서버 cron 1분마다 `tools/vault_build.py` → 내용이 바뀐 것만 커밋·푸시. 로컬 PC 는 1분마다 pull.
- **관제 아티팩트** · `dashboard/live.json` 을 Claude 세션이 심어 발행. 실행 상태·논문 대조·에이전트·연구 노트 4탭.
- 실험은 돌리지 않았다. GPU 2장 유휴. 마지막 정식 실행은 09-14 20:04 `edge-villain-six-gpu1-20260913` (VilLain HP 6개 데이터셋 완료).

## 결과

- 원장 기준 NC 90/106 · HP 89/105 셀이 논문 ±2 이내. 이상치 22칸 (GGD HP 전반, HyperGCL NC/HP, SE-HSSL×House NC +12.7, TriCL HP House −5.4 등) — `knowledge/논문 대조.md`.
- 논문 기준값 검증: 원장 Δ 211칸 전부 `paper_reference.json` 과 일치.

## 판단

- 다음 실험 우선순위는 `lab/다음 할 일.md` 에 시드해 뒀다: HyperGCL HP Pubmed·AMiner → VilLain HP 게이트 → Table 5.
- 이상치 중 코드 원인이 의심되는 것(TriCL `num_edges` 확장, VilLain `emb_concat.py` 부분일치 필터)은 `lab/질문.md` 에 올렸다. 재실행보다 원인 확인이 먼저.

## 커뮤니티 탐지 (Table 5) 착수

- 디스코드 `@지시` 로 받은 일. 저장소에 커뮤니티 탐지 경로가 없어 `tools/cluster_eval.py` 를 새로 만들었다.
  각 모델이 만든 노드 임베딩을 k-means 로 군집하고 레이블과의 NMI 를 잰다. 군집 수는 레이블 고유값 수.
  정식 규약이 20 seed 를 요구하므로 k-means random_state 0..19 로 20번 돌려 평균±표준편차를 남긴다.
- 모델 코드는 최소로 건드렸다. MaskGAE · H-GD · TriCL · GraphMAE2 에 `--save-emb` 만 추가했다.
  주지 않으면 원래 동작과 같다. 학습 설정은 각 모델 `118.sh` 그대로다.
- VilLain 은 학습을 다시 하지 않았다. 이미 저장된 merged 임베딩을 118.sh 설정대로 골라 썼다.

## 결과 — 4개 모델 × 6개 데이터셋

| 모델 | Citeseer | Cora-CA | IMDB | House | Pubmed | AMiner |
|---|---|---|---|---|---|---|
| MaskGAE | +0.4 낮음 | +1.4 | +0.3 | −1.2 | +1.0 | −0.0 |
| GGD | +1.7 | +0.7 | +0.0 | +0.1 | −2.7 | −0.7 |
| TriCL | +0.5 | +4.3 | −0.1 | −0.2 | −1.7 | −0.2 |
| VilLain | −2.2 | −0.0 | −0.1 | +0.0 | +0.0 | +1.5 |

24칸 중 22칸이 |Δ| 2 이내다. TriCL × Cora-CA (+4.3) 와 GGD × Pubmed (−2.7) 가 가장 벌어졌다.

## 막힌 것

- **GraphMAE2** — `hgnn-dgl-src` 환경이 `/tmp/dgl-v2.1.0-src-r2` 의 소스 빌드를 가리키는데 그 경로가 사라졌다.
  재부팅으로 /tmp 가 비워진 것으로 보인다. 환경 복구가 먼저다. HyperGRL 도 같은 환경이라 같이 막힌다.
- **HyperGCL · SE-HSSL** — `118.sh` 가 없어 정식 고정 설정이 저장소에 정의돼 있지 않다.
- **HypeBoy** — 학습 코드 구조가 달라 임베딩을 꺼낼 지점을 아직 못 찾았다.
- **데이터셋** — DBLP-A · Cora · MN-40 · 20News · DBLP-P 는 `data/` 에 파일이 없다. 어느 모델이든 6개까지만 된다.

## 판단

- 원장 Table 5 의 열이 Cora-CA · DBLP-P · 20News 뿐이어서 나머지 값이 '열 없음' 으로 버려지고 있었다.
  열을 6개 데이터셋으로 넓히고 논문 T5 의 9개 모델을 모두 행으로 세웠다.
- 결과 파일 이름은 `H-GD` 가 아니라 `HGD` 여야 원장 파서가 GGD 행으로 옮긴다. 저장소 관례다.

## 환경 복구 — hgnn-dgl-src

- dgl 2.1.0 이 `/tmp/dgl-v2.1.0-src-r2` 에 editable 로 걸려 있었고, 빌드 산출물(`libdgl.so`,
  `libgraphbolt_pytorch_2.7.0.so`)이 재부팅으로 사라져 import 부터 실패했다.
- 소스와 두 라이브러리를 `~/opt/dgl-v2.1.0-src` 로 옮기고 editable finder 경로를 그쪽으로 돌렸다.
  이제 `/tmp` 가 비워져도 깨지지 않는다. 확인: dgl 2.1.0 · torch 2.7.0+cu128 · cuda True.

## 커뮤니티 탐지 — 6개 모델 36칸

GraphMAE2 · MaskGAE · GGD · TriCL · HypeBoy · VilLain 이 6개 데이터셋에서 끝났다.
36칸 중 32칸이 |Δ| 2 이내다. 벌어진 칸은 넷이다.

- **GraphMAE2 × Pubmed +14.7** (34.7 vs 19.99) — 같은 모델의 다른 다섯 칸이 ±0.3 이내라
  평가 방법 문제는 아니다. 그 데이터셋 설정을 따로 봐야 한다.
- TriCL × Cora-CA +4.3 · HypeBoy × Cora-CA +2.8 — 두 모델이 같은 방향으로 벌어진다.
- GGD × Pubmed −2.7 · HypeBoy × Citeseer −2.2 · VilLain × Citeseer −2.2

## 아직 못 하는 3개 모델

- **HyperGRL** — 노드 임베딩을 만들지 않는다. 레이블을 직접 예측하는 scorer 구조라
  k-means 를 붙일 지점이 없다. `lab/질문.md` 의 열린 질문과 같은 벽이다.
- **HyperGCL · SE-HSSL** — `118.sh` 가 없어 정식 고정 설정이 저장소에 정의돼 있지 않다.
